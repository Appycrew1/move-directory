// This hook is integrated into the FeatureFlagProvider component  
// You can extract it if needed, but it's already included in the provider
export { useFeatureFlag, useFeatureFlags } from '@/components/providers/FeatureFlagProvider';
